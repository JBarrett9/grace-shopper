const { addToppingtoPizza } = require("../../db/pizza_toppings");

describe("DB Pizza Toppings", () => {
  describe("getPizzaToppingById", () => {
    it("Returns pizza topping", async () => {});
  });
});
